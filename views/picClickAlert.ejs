<%- include('partials/header') %>

<script>

    // var keyEvent = new KeyboardEvent("keypress", {key : "p", char : "p", ctrlKey: true, shiftKey: true});
    // document.dispatchEvent(keyEvent);
    alert('Press spacebar to click your pic');
    
</script>

<%- include('partials/footer') %>